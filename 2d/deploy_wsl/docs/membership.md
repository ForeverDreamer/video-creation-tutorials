# Where the full workflow lives

This demo gets you the standalone 3D text flythrough hook (basic tier) running in After Effects.

The deployment video walks through standing up the **whole 2D AE workflow** that produces episodes like this one. That complete, reusable setup ships with the **2D membership**:

- the shared `adobe/ae/scripts/common/` library (cameras, easing, 3D layout, render helpers) reused across every episode — not just this hook
- the project code, environment setup, and Adobe CEP bridge that drive After Effects from a script
- the CLI workflow and Claude Code integration shown in the video
- step-by-step deployment tutorials and ongoing updates

## Subscribe to the 2D membership

- Whop — subscription link in the video description.
